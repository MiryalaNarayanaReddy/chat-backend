import UserModel from "../model/User"

export const signup = (req,res)=>{
    // get username and password hash password using bcrypt and save it in usermodel 


}

export const login = (res,res)=>{

    // search database if user is present and if so then check if password matches and then return token and success message.

}